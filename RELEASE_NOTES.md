# Release Notes

## v0.3.0 (24) 测试构建

发布日期：2026-08-17

- 在线课堂分享区增加本地二维码生成，可直接用微信扫一扫进入学员页。
- 二维码在 Mac 本地通过 Core Image 生成，不调用腾讯云、不上传链接、不产生云费用。
- 二维码弹窗保留复制链接、打开学员页和关闭操作，并显示完整学员地址供核对。
- 继续保持 `ZERO_COST_MODE=true`、`TRTC_ENABLED=false`、`PAYMENTS_ENABLED=false`。

## v0.2.0-test

发布日期：2026-08-13

GitHub Release：`https://github.com/ljkeee6986/FlowRecorder/releases/tag/v0.2.0-test`

下载附件：

- `Jack-v0.2.0-macOS-arm64.dmg`
- `Jack-v0.2.0-macOS-arm64.zip`
- `SHA256SUMS.txt`

这是当前稳定测试版，核心目标是解决“录完后 MP4 损坏/打不开”的问题，并补齐录制前检查、保存状态、诊断和误退出保护。

### 关键修复

- 改用 `SCStream + SCRecordingOutput` 原生 MP4 输出，替换不稳定的自写 `AVAssetWriter` 路径。
- 停止录制后等待原生封装完成，再验证 MP4 可播放。
- 输出先写入 `.in-progress` 临时目录，验证成功后才移动到最终目录。
- 启动时自动隔离上次未完成的临时文件到 `损坏录屏` 目录。
- 录制中退出会先安全保存；保存中退出会被拦截，避免强退生成损坏文件。
- 开启音频源时校验 MP4 至少包含 1 条音轨。

### 新增能力

- 悬浮录制控制条，且不会被录入视频。
- 鼠标点击高亮后处理；失败时保留可播放原视频。
- 录制前健康检查：输出目录、磁盘空间、屏幕录制权限、麦克风权限。
- 健康检查异常时提供直接修复入口。
- 保存阶段状态提示：封装、验证、后处理、写入最终文件、清理临时文件。
- 复制诊断：包含版本、系统、App 路径、最近录屏、临时目录、隔离文件和日志尾部。
- 诊断日志尾部读取限流，并自动轮转 `status.txt`。
- 设置持久化：系统声音、麦克风、点击提示、导出清晰度、麦克风设备、提词器设置。
- 界面显示测试版号：`测试版 v0.2.0 (20)`。

### 稳定性加固

- 录制前检查输出目录是否可写。
- 录制前检查磁盘空间至少保留 500MB。
- 原生封装等待时间延长到 60 秒，降低长录屏误判失败概率。
- 窗口录制开录前必须确认 live window 仍存在，不再使用旧窗口位置兜底。
- 区域预设基于屏幕可见区域居中，避开菜单栏和 Dock。
- 长期日志不会无限增长。

### 已验证

- 多条真实短录屏通过 `ffprobe` 校验。
- 系统声音开启时输出 H.264 + AAC。
- 系统声音和麦克风全关时允许纯视频 H.264。
- 720P 后处理输出可播放。
- 录制中按 `Command + Q` 会先保存成功再退出。
- `.in-progress` 在正常保存后无残留。

### 待人工确认

- `9:16 竖屏` 区域输出比例。
- `1:1 方形` 区域输出比例。
- 麦克风开启后的人声可听性。
- 真实手点鼠标时点击光圈是否按预期叠加。

### 已知限制

- 仅支持 macOS 15+。
- 当前测试包未做 DMG / notarization。
- 摄像头小窗尚未作为独立视频层合成。
- 窗口录制本质是全屏捕获后裁剪，录制中移动窗口会影响结果。
